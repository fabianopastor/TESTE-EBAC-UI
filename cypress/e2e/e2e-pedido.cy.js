///<reference types="cypress"/>

describe('Teste de ponta a ponta em SauceLabs', () => {
    
    /*
    it('Deve fazer o pedido do comeco ao fim, inicialmente entrar no site', () => {
        cy.visit("https://www.saucedemo.com/")
        //login
        cy.login('standard_user','secret_sauce')

        //add produto
        cy.adicionarProdutos('Sauce Labs Backpack')
        //cy.adicionarProdutos('Sauce Labs Bike Light')
       
        //clicar no carrinho
        cy.get('.shopping_cart_link').click()
        cy.get('[data-test="checkout"]').click()
        cy.get('[data-test="firstName"]').type('Fabiano')
        cy.get('[data-test="lastName"]').type('Duarte')
        cy.get('[data-test="postalCode"]').type('23045060')
        cy.get('[data-test="continue"]').click()
        cy.get('[data-test="finish"]').click()
        cy.get('.complete-header').should('contain','Thank you for your order!')
       
    });
    */
   
    
});